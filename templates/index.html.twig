{% extends "base.html.twig" %}

{% block body %}
{# <a href="{{ path('teste')}}"><button type="submit">Teste Api</button></a> #}
{# <a href="{{ path('creation_equipe') }}"><button type="submit">Création d'équipe</button></a> #}
    <h1 class="test1">Pokéquipe</h1>
    
    <div class="containerMain">
            {% for data in datas %}
                <div class="titre">
                    <a href="{{ path('choice',{'id':data.id} ) }}"><img src="{{asset(data.img)}}">
                    <h2>{{ data.name }}</h2></a>
                </div>
            {% endfor %}
    </div>
<a href="{{ path('creation_equipe') }}"><button type="submit">Création d'équipe</button></a>
<a href="{{ path('liste_equipe') }}"><button type="submit">Liste Équipes</button></a>
{% endblock %}